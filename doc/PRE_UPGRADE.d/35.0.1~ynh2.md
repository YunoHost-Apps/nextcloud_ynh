Breaking change: If users' home directories in Nextcloud were enabled during install, this upgrade will fix their permissions, which **may take a while for users with many files**.

Afterwards, the `Home` external mount will point to `/home/$user/$app_home` instead of `/home/$user`. Use the button in the configuration panel to move users' existing files to the new path.
